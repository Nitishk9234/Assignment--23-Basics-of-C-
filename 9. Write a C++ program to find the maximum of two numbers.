#include <iostream>
using namespace std;
int main()
{
   int a,b;
    cout<<"enter two number:";
    cin>>a>>b;
    if(a>b)
        cout<<"a is max "<<a;
    else
        cout<<"b is max "<<b;
    return 0;
}
